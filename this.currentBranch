checkout(branch) {
    if (this.branches.hasOwnProperty(branch)) {
      this.currentBranch = branch;
      console.log(`Switched to branch: ${branch}`);
    } else {
      console.log(`Branch '${branch}' does not exist`);
    }
